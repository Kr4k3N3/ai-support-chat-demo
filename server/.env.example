OPENAI_API_KEY=your_key_here
PORT=8787
ALLOWED_ORIGIN=http://localhost:5173

# Email for escalations (who receives support requests)
TEAM_EMAIL=support@yourbusiness.com

# SMTP settings (optional - if not set, escalations are logged to console)
# For Gmail: use smtp.gmail.com, port 587, and an App Password
# For Outlook: use smtp.office365.com, port 587
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
